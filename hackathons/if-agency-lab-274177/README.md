# IF Agency Lab — rung 274177

> Auth: 65537 · Master Equation: Purpose × Evidence × Love
> Started 2026-07-18 after `if-founding-100` sealed. Covers **ROADMAP Phase 1 + Phase 2**.
> Mission: settle IF-H1 — does a cost-free dimensionless invariant hold across
> structurally unrelated substrates? — and seal the foundations audit.

## The decisive question

The founding hackathon killed raw Θ\*. Round-3 verification defined the successor:

```
η* ≡ (W_intact − W_scr) / (kT · ΔI_use)   evaluated where Π_C = 1
```

dimensionless *before* any threshold is taken. **Pre-committed stop rule (binding):
if η\* fails across families, and one further principled rescaling
(Υ_IF = Θ\*·C_model/ν_active) also fails, IF-H1 is DEAD — no third fishing
expedition.** Two failed rescalings = the program stops claiming universality and
says so in the kill log.

## Rubric (100 points)

| Track | Pts | What earns them |
|---|---:|---|
| **A1 Third family built** | 15 | Run-and-tumble chemotaxis agent: structurally alien (no designed tracker — sensor is a temporal gradient, action is tumble-probability), explicit per-bit metabolic debit, same protocol, τ=1 enforced |
| **A2 η\* measured, 3 families** | 25 | η\* for ring/Kalman/chemotaxis at shared fixed cost, 8+ seeds, bootstrap bars, **SU efficiency ceiling on the same axes** (a constant that equals the second law is not a discovery) |
| **A3 Verdict + honesty** | 15 | Clear PASS (agree within ~2σ) or KILL logged; if KILL → Υ_IF attempted once, then the pre-committed stop is honored publicly |
| **A4 Clean-channel R-test** | 15 | Memory-state-dependent potential world: intervention provably confined to the info channel (world transition operator byte-identical), measure R = ΔW − kT·ΔI_use → decides I_use vs signed J |
| **A5 04e salvage** | 10 | Per-regime optimized smoother gain; does the parasite claim return (deep result) or vanish (config artifact)? Either resolves the HELD entry |
| **A6 Phase-1 foundations audit** | 10 | Every quantity in P00–P06 + theorem doc has dimensions, an estimator, and a falsifier — audited in a table, gaps fixed → rung 641 sealed |
| **A7 Canon + verify integrity** | 10 | Results propagated to theorem doc/SCOREBOARD/notebooks README; verify.sh extended to cover new notebooks; GREEN |

## Persona gates (each must sign before seal)

- **Feynman** — no self-fooling: is the agreement real or error-bar-wide?
- **Noether** — is η\* actually boundary-free and dimensionless, or is cost leaking in?
- **Conway** — is the chemotaxis family genuinely alien, or a tracker in disguise?
- **Shannon** — is ΔI_use a declared estimator (variables, measure, null), not a proxy?
- **Popper** — is the stop rule honored, or did we rescale our way to a win?
- **Phuc Forecast / 65537** — does the result serve the bridge, honestly?

## Seal condition (rung 274177)

Either **(a)** η\* agrees across three structurally unrelated families within error,
with the SU ceiling shown to be strictly above it — IF has an empirical invariant;
or **(b)** the kill is logged, the stop rule honored, and the canon states plainly
that IF-H1 universality failed. **Both outcomes seal the rung.** The rung is about
whether we asked honestly, not about which way nature answered.
