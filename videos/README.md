# IF Theory — result films

Each film is tied to a sealed result. Rendered by `scripts/video.py`
(deterministic replay — same seeds as the sealed evidence JSONs).
Palette: teal = resource field, gold = living cells, magenta = tracked emergent movers.

| Film | Result it shows |
|---|---|
| `mobility-regime.mp4` | The sealed D4 regime (`if-mobility-search`): random soup, Conway-gate rules, no agency anywhere — and mobile structures emerge and travel (magenta trails). Seed 101, 28 movers. |
| `starved-vs-abundant.mp4` | The still-life mystery solved. Same rules, same seed; left E_BIRTH=1.0/inflow=0.9 freezes into scattered still lifes, right E_BIRTH=0.25/inflow=12 grows movers. Energy economics, not rules. |
| `scramble-ignition.mp4` | The audit confound, watchable (seed 42, sealed W_C=−39.4 row): the mover's cells shuffled once in the red box at t=0 — the disorganized debris ignites growth that out-harvests the organized mover. Why raw harvest is the wrong observable. |
